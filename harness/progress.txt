# Employment QA MVP Rebuild v2 Progress Log

## Context

- repo root: `/Users/bran/chat/employment-qa-mvp`
- active worktree: `/Users/bran/chat/worktrees/employment-qa-mvp/v2-polish`
- active branch: `v2-polish`
- reference donor: `/Users/bran/chat/reference-repos/vercel-chatbot`

## Session: V2 Rebuild Execution (2026-04-13)

### Completed Features
- `H1` complete: cloned and audited `vercel/chatbot`, then documented transplant versus discard decisions in `docs/reference-audit.md`.
- `H2` complete: replaced the minimal shell with a Tailwind + shadcn-style design system and shared product shell.
- `H3` complete: rebuilt `/` and `/qa` into a real product flow with starter prompts, multi-message chat, streaming output, and concise source tags.
- `H4` complete: added `POST /api/v1/qa/stream` while keeping `POST /api/v1/qa/ask` as the compatibility fallback.
- `H5` complete: replaced mock retrieval with real official-doc and graduate-data ingestion plus official-first answer composition.
- `H6` complete: rebuilt `/admin` into a token-based console for uploads, reindexing, and sync visibility.
- `H7` complete: refreshed backend tests, frontend tests, frontend build, docker compose validation, and visual captures.

### Important Implementation Notes
- The backend retrieval layer now uses `backend/app/retrieval/store.py`, a local persisted vector-like store built for stable MVP behavior.
- This deliberately supersedes the earlier placeholder/mock answer path.
- Official-source precedence remains absolute. Graduate data only appears as `经验参考`, and only when it does not conflict with official content.

### Verification
- `cd backend && . .venv/bin/activate && pytest -q` -> `10 passed`
- `cd frontend && npm test -- --runInBand` -> `4 passed, 5 tests`
- `cd frontend && npm run build` -> success with `/`, `/qa`, `/admin`
- `docker compose config` -> valid output

### Visual Capture Assets
- `docs/screenshots/home.png`
- `docs/screenshots/qa.png`
- `docs/screenshots/admin.png`

### Current Focus
- `H8`: finish delivery hygiene by committing the verified branch state and pushing once a remote is configured.

### Blockers
- `git remote -v` is empty, so branch push and remote handoff cannot finish yet.

## Session: Full-Site Gap Planning and Harness Sprint Setup (2026-04-13)

### Completed This Session
- Configured remote:
  - `origin -> https://github.com/chuxainpei/creer.git`
- Added public UX polish and removed admin entry from public header on branch `v2-polish`.
- Added and validated frontend test for no public admin navigation.
- Cloned additional UI references:
  - `/Users/bran/chat/reference-repos/chatbot-ui`
  - `/Users/bran/chat/reference-repos/vercel-ai-chatbot-supabase`
  - `/Users/bran/chat/reference-repos/lobe-chat`
  - `/Users/bran/chat/reference-repos/creer` (empty repo)
- Wrote full-site implementation plan:
  - `docs/superpowers/plans/2026-04-13-full-site-launch-readiness-design.md`
- Reset harness backlog with new features `F1-F8` for automated execution.

### Gap Analysis: Remaining Work to Reach a Complete Website
- Security boundary hardening for admin route discoverability and access gating.
- Ingestion governance quality (invalid files, duplicate policy, robust upload errors).
- Source explainability depth (file-level citation snippets in answer cards).
- Runtime reliability (rate-limit, timeout, retry behavior).
- Observability (request-id and structured operation logs).
- CI/CD and deployment hardening (workflow + security/cache proxy config).
- Launch-level UX completeness (service notice, FAQ/help/contact).
- Final release evidence and handoff package.

### Verification
- `cd frontend && npm test -- --runInBand` -> `5 passed, 6 tests`
- `cd frontend && npm run build` -> success

### Blockers
- Push attempt failed in current environment:
  - `git push -u origin v2-polish`
  - error: `LibreSSL SSL_connect: SSL_ERROR_SYSCALL in connection to github.com:443`

## Session: F1 Security Boundary Execution (2026-04-13)

### Completed Tasks
- Verified push blocker root cause moved from SSL to GitHub credential mode; switched remote to SSH and pushed `v2-polish` successfully.
- Implemented admin route gate:
  - added `frontend/middleware.ts` to protect `/admin` with cookie + entry-token handshake
  - added `frontend/src/lib/admin-gate.ts` for deterministic access-decision logic
- Added failing-first tests and made them pass:
  - `frontend/__tests__/admin-gate.test.ts`
  - existing `frontend/__tests__/site-header.test.tsx` remains green
- Marked `F1` as complete; next pending feature is `F2`.

### Verification
- `cd frontend && npm test -- --runInBand __tests__/admin-gate.test.ts __tests__/site-header.test.tsx` -> `2 passed`
- `cd frontend && npm test -- --runInBand` -> `6 passed, 9 tests`
- `cd frontend && npm run build` -> success, middleware compiled
- `git -C /Users/bran/chat/worktrees/employment-qa-mvp/v2-polish push -u origin v2-polish` -> pushed and tracking set

### Current Focus
- `F2`: ingestion quality gate (invalid file, empty file, duplicate policy).

### Blockers
- None for local development execution.

## Session: F2 Ingestion Quality Gate Execution (2026-04-13)

### Completed Tasks
- Added failing-first admin ingestion tests:
  - unsupported official file rejection
  - empty graduate upload rejection
  - duplicate official upload returns `replaced` flag
- Added retrieval test for invalid graduate file skip tracking.
- Implemented upload governance in admin ingestion endpoint:
  - empty payload rejection
  - max-size guard
  - staged-file validation before persistence
  - duplicate replacement detection (`replaced: true/false`)
  - explicit error messages for invalid official/graduate files
- Marked `F2` as complete; next pending feature is `F3`.

### Verification
- `cd backend && /Users/bran/.config/superpowers/worktrees/employment-qa-mvp/v2-rebuild/backend/.venv/bin/python -m pytest tests/test_admin_ingestion_api.py tests/test_retrieval_service.py -q` -> `7 passed`
- `cd backend && /Users/bran/.config/superpowers/worktrees/employment-qa-mvp/v2-rebuild/backend/.venv/bin/python -m pytest -q` -> `14 passed`

### Current Focus
- `F3`: source evidence explainability contract.

### Blockers
- None.

## Session: F3 Explainability Evidence Contract Execution (2026-04-13)

### Completed Tasks
- Added failing-first tests to require source evidence in both contracts:
  - `POST /api/v1/qa/ask` must return `evidence[]` with `source_name` and `snippet`
  - `POST /api/v1/qa/stream` metadata event must include `evidence`
- Extended backend response model and retrieval pipeline:
  - new response schema field `evidence`
  - context policy now returns selected `official_hits` and conflict-filtered `graduate_hits`
  - retrieval service builds student-facing evidence entries with source name + snippet
- Extended frontend chat model and rendering:
  - stream metadata and fallback ask responses now carry `evidence`
  - assistant message card now shows `来源证据` section under source tags
- Marked `F3` as complete; next pending feature is `F4`.

### Verification
- `cd backend && /Users/bran/.config/superpowers/worktrees/employment-qa-mvp/v2-rebuild/backend/.venv/bin/python -m pytest tests/test_public_qa_api.py -q` -> `2 passed`
- `cd frontend && npm test -- --runInBand __tests__/chat-shell.test.tsx` -> `1 passed`

### Current Focus
- `F4`: lightweight rate-limit, timeout, and retry resilience.

### Blockers
- None.

## Session: F4 Reliability Guardrails Execution (2026-04-13)

### Completed Tasks
- Added failing-first reliability tests for:
  - public ask rate-limit response (`429`)
  - public ask timeout fallback (`504`)
- Implemented backend guardrails in public QA endpoints:
  - in-memory per-client lightweight rate limiting for `/api/v1/qa/ask` and `/api/v1/qa/stream`
  - timeout guard for answer generation before response/stream dispatch
  - consistent Chinese error details for overload and timeout scenarios
- Extended retrieval stream path to accept a precomputed answer result, so stream endpoint can enforce timeout before opening SSE.
- Added frontend transient retry/backoff transport helper for ask/stream requests (`408/429/5xx` retryable statuses).
- Marked `F4` as complete; next pending feature is `F5`.

### Verification
- `cd backend && /Users/bran/.config/superpowers/worktrees/employment-qa-mvp/v2-rebuild/backend/.venv/bin/python -m pytest tests/test_public_qa_api.py -q` -> `4 passed`
- `cd backend && /Users/bran/.config/superpowers/worktrees/employment-qa-mvp/v2-rebuild/backend/.venv/bin/python -m pytest tests/test_retrieval_service.py -q` -> `2 passed`
- `cd frontend && npm test -- --runInBand` -> `9 passed`

### Current Focus
- `F5`: request-id propagation and structured operation logs.

### Blockers
- None.

## Session: F5 Observability Request-ID + Structured Logs (2026-04-13)

### Completed Tasks
- Added failing-first request-id propagation test for `POST /api/v1/qa/ask` and `POST /api/v1/qa/stream`.
- Implemented request-id middleware in FastAPI:
  - accept incoming `x-request-id` or generate a server id
  - bind request id to request context and return it in response headers
- Added structured logging helper (`app/observability.py`) and applied it to core operations:
  - `qa.ask.success|failed`
  - `qa.stream.opened|failed`
  - `admin.upload.official.success`
  - `admin.upload.graduate.success`
  - `admin.reindex.completed`
- Marked `F5` as complete; next pending feature is `F6`.

### Verification
- `cd backend && /Users/bran/.config/superpowers/worktrees/employment-qa-mvp/v2-rebuild/backend/.venv/bin/python -m pytest tests/test_public_qa_api.py -q` -> `5 passed`
- `cd backend && /Users/bran/.config/superpowers/worktrees/employment-qa-mvp/v2-rebuild/backend/.venv/bin/python -m pytest tests/test_admin_ingestion_api.py tests/test_retrieval_service.py -q` -> `7 passed`
- `cd frontend && npm test -- --runInBand` -> `9 passed`

### Current Focus
- `F6`: CI workflow + deployment hardening baseline.

### Blockers
- None.

## Session: F6 CI + Deployment Hardening Baseline (2026-04-13)

### Completed Tasks
- Added CI workflow at `.github/workflows/ci.yml` to run:
  - backend pytest
  - frontend jest
  - frontend production build
  - `docker compose config`
- Hardened `deploy/nginx.conf`:
  - added security headers (`X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`)
  - configured SSE route `/api/v1/qa/stream` with `proxy_buffering off` + timeout + no-store cache
  - configured immutable static cache for `/_next/static/`
  - set no-store cache for API proxy routes
- Updated `docs/deployment.md` with CI baseline and Nginx hardening notes.
- Marked `F6` as complete; next pending feature is `F7`.

### Verification
- `cd backend && /Users/bran/.config/superpowers/worktrees/employment-qa-mvp/v2-rebuild/backend/.venv/bin/python -m pytest -q` -> `17 passed`
- `cd frontend && npm test -- --runInBand` -> `9 passed`
- `cd frontend && npm run build` -> success
- `docker compose config` -> valid output

### Current Focus
- `F7`: launch UX content completion (notice/help/FAQ/support blocks).

### Blockers
- None.

## Session: F7 Launch UX Content Completion (2026-04-13)

### Completed Tasks
- Added failing-first homepage launch-content assertions (`服务公告`, `使用指南`, `常见问题`, `支持与反馈`).
- Reworked homepage launch blocks in `EntryModule` with complete, student-facing guidance content and clear support language.
- Added QA-page companion notice/help/support cards to keep message consistency after进入问答页.
- Marked `F7` as complete; next pending feature is `F8`.

### Verification
- `cd frontend && npm test -- --runInBand __tests__/home-page.test.tsx` -> `1 passed`
- `cd frontend && npm test -- --runInBand` -> `9 passed`
- `cd frontend && npm run build` -> success

### Current Focus
- `F8`: final release evidence (full verification, fresh screenshots, README/progress handoff).

### Blockers
- None.

## Session: F8 Release Evidence + Handoff Finalization (2026-04-13)

### Completed Tasks
- Executed full launch verification suite (backend/frontend/build/compose).
- Refreshed release screenshots:
  - `docs/screenshots/home.png`
  - `docs/screenshots/qa.png`
  - `docs/screenshots/admin.png`
- Updated `README.md` with launch readiness status, release artifacts, and known limitations.
- Marked `F8` as complete. Harness backlog `F1-F8` is now fully passed.

### Verification
- `cd backend && /Users/bran/.config/superpowers/worktrees/employment-qa-mvp/v2-rebuild/backend/.venv/bin/python -m pytest -q` -> `17 passed`
- `cd frontend && npm test -- --runInBand` -> `9 passed`
- `cd frontend && npm run build` -> success
- `docker compose config` -> valid output

### Current Focus
- Delivery complete for current harness scope. Next cycle can start from post-launch feedback backlog.

### Blockers
- None.
