services:
  - type: web
    name: employment-qa-demo-api
    runtime: python
    rootDir: demo-api
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ALLOWED_ORIGINS
        value: ""
